 
 ## Features
 - I want to be able to do the data sync with gitea as well instead of just GitHub. Review the documentation below and provide a setting to use Gitea instead. I should just have to provide the base url, user, repo and api key. Documentation at https://gitea.com/api/swagger
 - Add the ability to add additional matches to a game night even after a game night has been started. Currently once you add the matches to a night and stat you can’t add more matches. I want to be able to add matches even after a game night starts. 
 - phase 10 is a unique game because each round each player is on a different phase and accordingly it doesn’t have a definitive number of  rounds.  Search for and review the rules to phase 10 then add a special built in game for phase 10 that handles scoring and rounds for phase 10
 - Add the ability to capture additional stats for a round. For example. Five crowns I want to know which player went out first for each round. 

## BUGS:

- when adding a new user or new game type it adds multiple copies. I deleted a few users and added a new one and it added 6 copies. Similar happened when I added a new game. 
- 